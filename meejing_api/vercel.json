{
  "builds": [
    {
      "src": "meejing_api/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "buildCommand": "pip install -r requirements.txt && python manage.py makemigrations --noinput && python manage.py migrate --noinput && python manage.py collectstatic --noinput --clear",
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/staticfiles/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/meejing_api/wsgi.py"
    }
  ]
}

